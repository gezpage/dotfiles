#!/bin/bash

function output {
    message=$*
    echo -e "\e[01;33m${message}\e[00m"
}

for D in *;
do
    if [ -d $D ]; then
        cd $D;
        if [ -d .git ]; then
            ( echo -n "$D: " && output `git rev-parse --abbrev-ref HEAD` )
        fi;
        cd ..;
    fi;
done
